apiVersion: v1
kind: Secret
metadata:
  name: diving-analytics-secrets
type: Opaque
stringData:
  # IMPORTANT: Replace these placeholder values before deploying to production
  # Use kubectl create secret or external secret management (e.g., Vault, Sealed Secrets)
  database-url: "postgresql://user:password@postgres-service:5432/diving_analytics"
  redis-url: "redis://redis-service:6379"
  # Generate a secure JWT secret: openssl rand -base64 32
  jwt-secret: "REPLACE_WITH_SECURE_RANDOM_VALUE"
